# Legacy Reference Code

This directory contains the pre-rewrite implementation moved from `src/` and `tests/`.

Snapshot tag:

- `legacy-pre-rewrite-20260228`

Use this as reference only. New implementation should be developed under `src/` and `tests/`.
